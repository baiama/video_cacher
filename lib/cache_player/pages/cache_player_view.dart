import 'package:flutter/material.dart';

class CachePlayerView extends StatelessWidget {
  const CachePlayerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
